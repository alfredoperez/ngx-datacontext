import { RepositoryFactoryService } from "./repository-factory.service";

describe("RepositoryFactoryService", () => { 
    let service: RepositoryFactoryService;

    beforeEach(() => {
      //  service = new RepositoryFactoryService();
    });

    it("works", () => {
        expect(1).toEqual(2);
    });

});
